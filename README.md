This webapp uses ReactJS and the free Open Library API. Book Pal is a simple web interface that allows the user to search through the API on the basis of book title, author's name and genre and, returns the results in a structured manner, along with the respective book cover (Also using Open Library's Cover API).